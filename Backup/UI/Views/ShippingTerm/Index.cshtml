@using SCMS.Resource
@{
    ViewBag.Title = Resources.Global_String_ManageShippingTerms;
    Layout = "~/Views/Shared/_GeneralLayout.cshtml";
}

<div id="projectsMain" class="contentMain">
    <div class="contentHeader">
        <div class="headerText">
            <h3 class="title-left">
                @Resources.Global_String_ManageShippingTerms</h3>
        </div>
        <div style="font-size: 13px;">
            <table cellpadding="0" cellspacing="0">
                <tr>
                    <td class="arrowLink">
                        @Ajax.ActionLink(Resources.Global_String_AddNew, "Index", new { id = "CreateEdit" }, new AjaxOptions
                        {
                            UpdateTargetId = "popup-page",
                            LoadingElementId = "popup-loading",
                            Url = Url.Action("CreateEdit"),
                            OnBegin = string.Format("ShowDialogue('{0}')", Resources.Global_String_AddNew),
                            OnSuccess = "CenterPopup"
                        })
                    </td> 
                    <td class="arrowLink">
                        @Html.ActionLink(Resources.Global_String_BackToGlobalSettings, "Index", "GlobalSettings")
                    </td>
                </tr>
                <tr>
                    <td class="arrowLink">
                        @Ajax.ActionLink(Resources.Global_String_ShippingTerms, "Index", new AjaxOptions
                        {
                            UpdateTargetId = "ShipingDiv",
                            LoadingElementId = "loading",
                            Url = Url.Action("ListView"),
                            OnSuccess = "if (document.getElementById('table') != null) {initSorter();sorter.size(20);}"
                        })
                    </td>   
                </tr>
            </table>
        </div>
    </div>
    <br />
    <div id="loading" style="display: none; color: Blue; font-weight: bold;">
        <img src="../../Content/images/loading_indicator.gif" alt="Processing request..." />
    </div>
    <div id="ShipingDiv">
        @Html.Action("ListView")
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        if (document.getElementById('table') != null) {
            initSorter();
            sorter.size(20);
        }
    });
</script>
